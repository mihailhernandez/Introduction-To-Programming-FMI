#include <iostream>

/*
 * Дадени са 5 цели числа. Всяко число се среща 
 * четен брой пъти с изключение на едно! Да се 
 * изведе числото, което се среща нечетен брой пъти.
 */
int main()
{
    int a, b, c, d, e;

    std::cin >> a >> b >> c >> d >> e;

    std::cout << ((((a ^ b) ^ c) ^ d) ^ e) << std::endl;

    return 0;
}
